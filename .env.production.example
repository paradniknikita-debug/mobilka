# ============================================
# Production — docker-compose.prod.yml (корень проекта или сервер)
# ============================================
# Скопируйте в .env (или .env.production) и подставьте реальные значения.
# На сервере: скопируйте server-deploy-files/env.example в .env.
# Запуск: docker compose -f docker-compose.prod.yml --env-file .env up -d
# НЕ коммитьте .env / .env.production.

# Образ backend (на сервере — из GHCR; локально можно собрать свой)
BACKEND_IMAGE=ghcr.io/YOUR_GITHUB_USERNAME/mobilka-backend:latest

POSTGRES_DB=lepm_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=<надёжный пароль>

DATABASE_URL=postgresql://postgres:PASSWORD@postgres:5432/lepm_db
REDIS_URL=redis://redis:6379
SECRET_KEY=<сгенерируйте длинный ключ>
ENVIRONMENT=production
CORS_ORIGINS=https://your-domain.com
# Для теста по IP без домена можно временно: CORS_ORIGINS=*
TZ=Europe/Moscow

# Логи Docker в админ-панели (backend читает docker.sock на хосте)
ADMIN_DOCKER_LOGS_ENABLED=true
# GID группы docker на сервере: stat -c '%g' /var/run/docker.sock
DOCKER_GROUP_GID=999

# Ссылки в админ-панели (пусто — из Host браузера; можно задать явно)
# ADMIN_BACKEND_PUBLIC_URL=https://85.239.48.199
# ADMIN_MINIO_CONSOLE_URL=http://85.239.48.199:9001
