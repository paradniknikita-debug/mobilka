# Архитектура модуля подстанций и оборудования

## Обзор

Модуль для детального управления подстанциями и их оборудованием, отделенный от карты для лучшей организации информации.

## Архитектура решения

### Концепция: Разделение контекстов

```
┌─────────────────────────────────────────────────┐
│              ПРИЛОЖЕНИЕ                          │
├─────────────────────────────────────────────────┤
│                                                  │
│  ┌──────────────┐      ┌──────────────────┐   │
│  │   КАРТА      │      │  ДЕТАЛЬНЫЙ       │   │
│  │              │      │  ПРОСМОТР        │   │
│  │  - География │◄────►│                  │   │
│  │  - Навигация │      │  - Информация    │   │
│  │  - Минимум   │      │  - Оборудование  │   │
│  │    данных    │      │  - Характеристики│   │
│  └──────────────┘      └──────────────────┘   │
│                                                  │
└─────────────────────────────────────────────────┘
```

## Компоненты системы

### 1. Backend (FastAPI)

#### Модели данных

**Substation (расширенная)**
```python
class Substation(Base):
    # Существующие поля...
    substation_type = Column(String(20))  # ТП, КТП, МТП, РП
    equipment = relationship("Equipment", back_populates="substation")
```

**Equipment (расширенная)**
```python
class Equipment(Base):
    # Существующие поля...
    substation_id = Column(Integer, ForeignKey("substations.id"), nullable=True)
    substation = relationship("Substation", back_populates="equipment")
    
    # Новые поля
    equipment_category = Column(String(50))  # Трансформатор, Выключатель
    manufacturer = Column(String(100))
    model = Column(String(100))
    serial_number = Column(String(100))
    installation_date = Column(DateTime)
    last_maintenance_date = Column(DateTime)
    next_maintenance_date = Column(DateTime)
    status = Column(String(20))  # active, maintenance, decommissioned
    specifications = Column(JSON)  # Дополнительные характеристики
```

#### API Endpoints

```
# Подстанции
GET    /api/v1/substations                    # Список с фильтрацией
GET    /api/v1/substations/{id}              # Детальная информация
POST   /api/v1/substations                   # Создание
PUT    /api/v1/substations/{id}               # Обновление
DELETE /api/v1/substations/{id}              # Удаление

# Оборудование подстанции
GET    /api/v1/substations/{id}/equipment    # Список оборудования
GET    /api/v1/substations/{id}/equipment/{equipment_id}  # Детали оборудования
POST   /api/v1/substations/{id}/equipment    # Добавление оборудования
PUT    /api/v1/substations/{id}/equipment/{equipment_id}  # Обновление
DELETE /api/v1/substations/{id}/equipment/{equipment_id}  # Удаление

# Фильтрация и поиск
GET    /api/v1/substations?type=ТП           # По типу
GET    /api/v1/substations?region_id=1       # По региону
GET    /api/v1/substations?search=название   # Поиск
GET    /api/v1/substations?voltage_min=10&voltage_max=35  # По напряжению
```

### 2. Frontend (Angular)

#### Структура модулей

```
substations/
├── substations.module.ts
├── substations-routing.module.ts
│
├── substations-list/                    # Список подстанций
│   ├── substations-list.component.ts
│   ├── substations-list.component.html
│   └── substations-list.component.scss
│
├── substation-detail/                   # Детальный просмотр
│   ├── substation-detail.component.ts
│   ├── substation-detail.component.html
│   └── substation-detail.component.scss
│
├── substation-equipment/                 # Компонент оборудования
│   ├── substation-equipment.component.ts
│   ├── substation-equipment.component.html
│   └── substation-equipment.component.scss
│
├── equipment-form/                       # Форма создания/редактирования
│   ├── equipment-form.component.ts
│   ├── equipment-form.component.html
│   └── equipment-form.component.scss
│
└── services/
    └── substation.service.ts            # Сервис для работы с API
```

#### Компоненты

**SubstationsListComponent**
- Отображение списка подстанций
- Фильтрация по типу, региону, напряжению
- Поиск по названию/коду
- Переход в детальный просмотр

**SubstationDetailComponent**
- Основная информация о подстанции
- Вкладки: "Основная информация" / "Оборудование"
- Кнопки действий: Редактировать, Удалить
- Ссылка на карту

**SubstationEquipmentComponent**
- Таблица оборудования
- Группировка по категориям
- Фильтрация по статусу
- Добавление/редактирование оборудования

**EquipmentFormComponent**
- Форма создания/редактирования оборудования
- Валидация полей
- Сохранение в БД

#### Сервисы

**SubstationService**
```typescript
class SubstationService {
  getSubstations(filters?: SubstationFilters): Observable<Substation[]>
  getSubstation(id: number): Observable<Substation>
  createSubstation(data: SubstationCreate): Observable<Substation>
  updateSubstation(id: number, data: SubstationUpdate): Observable<Substation>
  deleteSubstation(id: number): Observable<void>
  
  getEquipment(substationId: number): Observable<Equipment[]>
  getEquipmentDetail(substationId: number, equipmentId: number): Observable<Equipment>
  createEquipment(substationId: number, data: EquipmentCreate): Observable<Equipment>
  updateEquipment(substationId: number, equipmentId: number, data: EquipmentUpdate): Observable<Equipment>
  deleteEquipment(substationId: number, equipmentId: number): Observable<void>
}
```

## Поток данных

### Сценарий: Просмотр оборудования подстанции

```
1. User → SubstationsListComponent
   ↓
2. User clicks on substation
   ↓
3. Router navigates to /substations/{id}
   ↓
4. SubstationDetailComponent loads
   ↓
5. SubstationService.getSubstation(id)
   ↓
6. Backend API → PostgreSQL
   ↓
7. SubstationDetailComponent displays info
   ↓
8. User clicks "Оборудование" tab
   ↓
9. SubstationEquipmentComponent loads
   ↓
10. SubstationService.getEquipment(substationId)
    ↓
11. Backend API → PostgreSQL
    ↓
12. Equipment list displayed
```

### Сценарий: Добавление оборудования

```
1. User in SubstationEquipmentComponent
   ↓
2. User clicks "Добавить оборудование"
   ↓
3. EquipmentFormComponent dialog opens
   ↓
4. User fills form
   ↓
5. User clicks "Сохранить"
   ↓
6. SubstationService.createEquipment(substationId, data)
   ↓
7. Backend API → PostgreSQL
   ↓
8. Equipment created
   ↓
9. Dialog closes, list refreshes
```

## UI/UX Дизайн

### Страница списка подстанций

```
┌─────────────────────────────────────────────────────┐
│  Подстанции                          [+ Создать]    │
├─────────────────────────────────────────────────────┤
│  [Тип: Все ▼] [Регион: Все ▼] [Поиск...] [🔍]      │
├─────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────┐ │
│  │ 🏭 ТП-123 "Центральная"              [⋮]      │ │
│  │ 10 кВ | Минск, ул. Ленина, 1                  │ │
│  │ Оборудование: 15 ед. | Статус: Активна        │ │
│  └───────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────┐ │
│  │ ⚡ КТП-456 "Промышленная"            [⋮]      │ │
│  │ 35 кВ | Минск, пр. Победителей, 5            │ │
│  │ Оборудование: 8 ед. | Статус: Активна        │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### Страница детального просмотра

```
┌─────────────────────────────────────────────────────┐
│  ← Назад    ТП-123 "Центральная"        [✏️] [🗑️]   │
├─────────────────────────────────────────────────────┤
│  [Основная информация] [Оборудование] [Подключения] │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌───────────────────────────────────────────────┐ │
│  │ Тип: ТП                                        │ │
│  │ Код: ТП-123                                    │ │
│  │ Напряжение: 10 кВ                              │ │
│  │ Адрес: Минск, ул. Ленина, 1                   │ │
│  │ Статус: Активна                                │ │
│  │ Подключенные ЛЭП: 3                            │ │
│  │ Координаты: 53.9045, 27.5615                  │ │
│  │                    [📍 Показать на карте]      │ │
│  └───────────────────────────────────────────────┘ │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### Вкладка "Оборудование"

```
┌─────────────────────────────────────────────────────┐
│  Оборудование (15 ед.)              [+ Добавить]   │
├─────────────────────────────────────────────────────┤
│  [Все категории ▼] [Статус: Все ▼] [Поиск...]      │
├─────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────┐ │
│  │ 🔌 Трансформатор ТМ-1000              [✏️]    │ │
│  │ Производитель: ABB | Модель: ТМ-1000/10       │ │
│  │ Серийный номер: SN-12345                       │ │
│  │ Статус: Активен | Установлен: 01.01.2020      │ │
│  │ Последнее ТО: 01.01.2024 | Следующее: 01.07.24│ │
│  └───────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────┐ │
│  │ ⚡ Выключатель ВМГ-10                 [✏️]    │ │
│  │ Производитель: Siemens | Модель: ВМГ-10       │ │
│  │ Серийный номер: SN-67890                       │ │
│  │ Статус: На обслуживании                        │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

## Интеграция с картой

### Popup на карте

При клике на подстанцию на карте показывать popup:

```
┌─────────────────────────────┐
│  ТП-123 "Центральная"       │
│  10 кВ                      │
│  Оборудование: 15 ед.       │
│                             │
│  [Подробнее] [На карте]     │
└─────────────────────────────┘
```

### Навигация

- Из карты → детальный просмотр: `/substations/{id}`
- Из детального просмотра → карта: центрирование на координатах
- Из списка → детальный просмотр: `/substations/{id}`
- Из детального просмотра → список: `/substations`

## Типы подстанций

### Классификация:

- **ТП** (Трансформаторная подстанция) - понижающая подстанция
- **КТП** (Комплектная трансформаторная подстанция) - закрытая подстанция
- **МТП** (Мачтовая трансформаторная подстанция) - открытая подстанция
- **РП** (Распределительный пункт) - без трансформаторов
- **ПС** (Подстанция) - общее название

### Категории оборудования:

- **Трансформаторы** - силовые трансформаторы
- **Выключатели** - автоматические выключатели
- **Разъединители** - коммутационные аппараты
- **Защита** - реле, автоматы защиты
- **Измерительное** - счетчики, трансформаторы тока/напряжения
- **Вспомогательное** - освещение, отопление, вентиляция

## Безопасность

- Проверка прав доступа при просмотре/редактировании
- Валидация данных на клиенте и сервере
- Логирование изменений оборудования
- Аудит действий пользователей

## Производительность

- Пагинация списка оборудования
- Ленивая загрузка данных
- Кэширование списка подстанций
- Оптимизация запросов к БД (eager loading)

## Тестирование

- Unit тесты для компонентов
- Integration тесты для API
- E2E тесты для пользовательских сценариев
- Тесты производительности
