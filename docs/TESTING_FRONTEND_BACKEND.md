# Тестирование взаимодействия Frontend ↔ Backend

## Что было добавлено

### 1. Тестовый endpoint в Backend
**Файл:** `backend/app/main.py`
```python
@app.get("/api/v1/test", tags=["test"])
async def test_endpoint(message: str = "Hello from backend!"):
    """Тестовый endpoint для проверки взаимодействия фронт-бэк"""
    return {
        "message": message,
        "timestamp": datetime.datetime.now().isoformat(),
        "backend_status": "✅ Backend работает!",
        ...
    }
```

**Использование:**
- Браузер: `https://localhost/api/v1/test`
- С параметром: `https://localhost/api/v1/test?message=Тестовое%20сообщение`

### 2. Тестовая страница во Frontend
**Файл:** `frontend/lib/features/home/presentation/pages/home_page.dart`

**Возможности:**
- ✅ Кнопка "Проверить Backend"
- ✅ Визуальное отображение ответа от backend
- ✅ Показ ошибок если что-то не работает
- ✅ Индикатор загрузки

## Как использовать

### Шаг 1: Запусти backend
```powershell
docker compose up -d
```

### Шаг 2: Запусти frontend
```powershell
cd frontend
flutter run -d edge
```

### Шаг 3: Тестирование
1. Открой приложение в браузере: `http://localhost:57005/` (или другой порт)
2. На странице Home нажми кнопку **"Проверить Backend"**
3. Увидишь:
   - ⏳ Индикатор загрузки во время запроса
   - ✅ Зелёный блок с ответом от backend (если успешно)
   - ❌ Красный блок с ошибкой (если что-то не работает)

## Что видно в ответе

**Успешный ответ:**
```json
{
  "message": "Hello from backend!",
  "timestamp": "2025-11-02T13:35:38.123456",
  "backend_status": "✅ Backend работает!",
  "request_received": true,
  "data": {
    "api_version": "v1",
    "server_time": "2025-11-02 13:35:38",
    "timezone": "UTC"
  }
}
```

**Ошибка (если что-то не так):**
- Сетевая ошибка (backend не запущен)
- CORS ошибка (проблема с CORS настройками)
- Timeout (backend слишком долго отвечает)
- 502 Bad Gateway (nginx не может достучаться до backend)

## Дополнительные проверки

### В браузере (DevTools)
1. Открой `F12` → вкладка **Network**
2. Нажми "Проверить Backend"
3. Увидишь запрос:
   - Метод: `GET`
   - URL: `https://localhost/api/v1/test`
   - Status: `200 OK` (или код ошибки)
   - Response: JSON с данными от backend

### В терминале backend
```powershell
docker compose logs -f backend
```
Увидишь логи запроса в реальном времени.

### В терминале nginx
```powershell
docker compose logs -f nginx
```
Увидишь проксированные запросы.

## Расширенное тестирование

### Тест с параметрами
Можешь модифицировать код, чтобы отправлять разные параметры:
```dart
final response = await dio.get(
  '/api/v1/test',
  queryParameters: {'message': 'Моё тестовое сообщение'},
);
```

### Тест POST запроса
Создай тестовый POST endpoint и проверь отправку данных:
```dart
final response = await dio.post(
  '/api/v1/test',
  data: {'test_field': 'test_value'},
);
```

## Что это показывает

✅ **Если видишь зелёный блок с данными:**
- Frontend успешно отправляет запрос
- Nginx проксирует запрос
- Backend получает и обрабатывает запрос
- Ответ возвращается через nginx
- Frontend получает и отображает ответ

**Всё работает! 🎉**

❌ **Если видишь ошибку:**
- Проверь логи backend и nginx
- Проверь что все контейнеры запущены: `docker compose ps`
- Проверь что frontend использует правильный URL (https://localhost)

