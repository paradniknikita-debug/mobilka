# Рекомендации по очистке проекта

## Файлы и папки, которые можно удалить

### ✅ Удалить (не нужны):

1. **`flutter/`** (весь Flutter SDK)
   - Это локальная копия Flutter SDK - огромная папка
   - Уже в `.gitignore`
   - Каждый разработчик устанавливает Flutter отдельно
   - **Размер:** ~2-3 GB

2. **`requirements.txt` в корне проекта**
   - Дублирует `backend/requirements.txt`
   - Используется только `backend/requirements.txt`

3. **`backend/app/nginx/nginx.conf`**
   - Устаревший файл, используется `nginx/nginx.conf` в корне
   - Может вызывать путаницу

4. **Старые .bat файлы (если не используются):**
   - `setup_project.bat` - если используешь `setup-docker.bat`
   - `start_backend.bat` и `start_frontend.bat` - если всё через docker-compose

### ⚠️ Оставить (могут пригодиться):

1. **`setup-docker.bat`** и **`fix-and-start.bat`**
   - Полезны для быстрого старта через Docker
   - Автоматизируют настройку

2. **`nginx/generate-ssl.bat`** и **`nginx/generate-ssl.sh`**
   - Нужны для генерации SSL сертификатов

3. **`backend/static/`** и **`static/`** (если используются)
   - Для статических файлов, загружаемых пользователями

4. **`DEPLOYMENT.md`**
   - Может содержать полезную информацию о деплое

### 📁 Структура после очистки:

```
mobilka/
├── backend/
│   ├── app/               # Код backend
│   ├── requirements.txt   # Зависимости Python
│   ├── Dockerfile
│   └── env_example.txt
├── frontend/
│   ├── lib/              # Код Flutter
│   ├── pubspec.yaml      # Зависимости Dart
│   └── ...
├── nginx/
│   ├── nginx.conf        # Конфигурация nginx
│   ├── generate-ssl.bat   # Генерация SSL
│   └── ssl/              # SSL сертификаты (не в git)
├── docs/                 # Документация
├── docker-compose.yml    # Docker конфигурация
├── .gitignore
└── README.md
```

## Команды для очистки

```powershell
# Удаление Flutter SDK (если хочешь освободить место)
# ВНИМАНИЕ: Убедись, что у тебя установлен Flutter глобально!
# Remove-Item -Recurse -Force flutter

# Удаление дубликатов
Remove-Item requirements.txt
Remove-Item backend\app\nginx\nginx.conf -ErrorAction SilentlyContinue

# Удаление старых батников (если не используешь)
# Remove-Item setup_project.bat
# Remove-Item start_backend.bat
# Remove-Item start_frontend.bat
```

## Что НЕ трогать:

- ❌ `backend/app/nginx/ssl/` - может содержать рабочие сертификаты
- ❌ `backend/.env` - никогда не коммить, но файл может быть нужен локально
- ❌ `frontend/build/` - сборки (уже в .gitignore)
- ❌ `__pycache__/` - кэш Python (уже в .gitignore)

