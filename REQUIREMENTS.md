# 📋 Требования для запуска проекта

## Минимальные требования

### Для запуска Backend (через Docker)

- **Docker Desktop** версии 20.10+ (Windows/Mac)
- **Docker Engine** 20.10+ и **Docker Compose** 2.0+ (Linux)
- **Git** для клонирования репозитория
- **4 GB RAM** минимум (рекомендуется 8 GB)
- **5 GB свободного места** на диске

### Для запуска Frontend (Flutter)

- **Flutter SDK** версии 3.0.0 или выше
- **Dart SDK** (входит в Flutter)
- **Android Studio** или **VS Code** с расширением Flutter (опционально)
- **Android SDK** (для Android разработки)
- **Xcode** (для iOS разработки на Mac)

## Проверка установки

### Docker

**Windows/Mac:**
```bash
docker --version
docker compose version
```

**Linux:**
```bash
docker --version
docker compose version
```

Если команды не найдены, установите:
- **Windows/Mac**: [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux**: Следуйте [официальной инструкции](https://docs.docker.com/engine/install/)

### Flutter (опционально, только для frontend)

```bash
flutter --version
flutter doctor
```

Если Flutter не установлен:
- **Windows/Mac/Linux**: [Установка Flutter](https://docs.flutter.dev/get-started/install)

## Порты, которые будут использоваться

- **80** - HTTP (редирект на HTTPS)
- **443** - HTTPS (основной)
- **5432** - PostgreSQL (для внешних подключений)
- **6379** - Redis (внутренний, не проброшен)

⚠️ **Важно**: Убедитесь, что эти порты свободны, или измените их в `docker-compose.yml`.

## Операционные системы

### Поддерживаемые ОС

- ✅ **Windows** 10/11 (64-bit)
- ✅ **macOS** 10.15+ (Intel/Apple Silicon)
- ✅ **Linux** (Ubuntu 20.04+, Debian 11+, Fedora 34+)

### Рекомендуемые ОС

- **Windows**: Windows 11
- **macOS**: macOS 12+
- **Linux**: Ubuntu 22.04 LTS

## Дополнительные инструменты (опционально)

- **DBeaver** или **pgAdmin** - для работы с базой данных
- **Postman** или **Insomnia** - для тестирования API
- **VS Code** или **Android Studio** - для разработки

## Проверка готовности

Перед запуском проекта выполните:

```bash
# Проверка Docker
docker --version
docker compose version

# Проверка Flutter (если нужен frontend)
flutter --version
flutter doctor
```

Если все команды выполняются без ошибок - вы готовы к запуску! 🚀

## Что делать, если что-то не работает?

1. **Docker не запускается**: Перезагрузите компьютер, убедитесь что виртуализация включена в BIOS
2. **Порты заняты**: Измените порты в `docker-compose.yml` или остановите конфликтующие сервисы
3. **Flutter не найден**: Добавьте Flutter в PATH или используйте полный путь к `flutter` команде

Смотри [README.md](README.md) для подробных инструкций по установке.

