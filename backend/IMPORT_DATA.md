# Импорт данных в базу данных

Система поддерживает два способа добавления данных:
1. **Массовый импорт из Excel** - для больших объёмов данных
2. **Ручное добавление через API** - для единичных записей

## 📊 Массовый импорт из Excel

### Способ 1: Через API (рекомендуется)

Используй Swagger UI для импорта:
1. Открой `https://localhost/api/docs` (или `http://localhost/api/docs`)
2. Найди раздел **"import"**
3. Выбери нужный endpoint:
   - `POST /api/v1/import/power-lines` - импорт ЛЭП
   - `POST /api/v1/import/poles` - импорт опор
   - `POST /api/v1/import/substations` - импорт подстанций
   - `POST /api/v1/import/equipment` - импорт оборудования
4. Нажми **"Try it out"**
5. Выбери Excel файл и загрузи
6. Получи результат с количеством импортированных записей и ошибками (если есть)

### Способ 2: Через скрипт (для Docker)

```bash
# Импорт ЛЭП
docker compose exec backend python import_from_excel.py power-lines /app/data/power_lines.xlsx

# Импорт опор
docker compose exec backend python import_from_excel.py poles /app/data/poles.xlsx

# Импорт подстанций
docker compose exec backend python import_from_excel.py substations /app/data/substations.xlsx

# Импорт оборудования
docker compose exec backend python import_from_excel.py equipment /app/data/equipment.xlsx
```

**Важно:** Файл должен быть скопирован в контейнер или смонтирован как volume.

### Формат Excel файлов

#### 1. Импорт ЛЭП (`power-lines`)

**Обязательные колонки:**
- `name` - Название ЛЭП (текст)
- `code` - Код ЛЭП (текст, уникальный)
- `voltage_level` - Уровень напряжения, кВ (число)

**Опциональные колонки:**
- `length` - Длина, км (число)
- `region_code` - Код географического региона (текст)
- `region_name` - Название географического региона (текст)
- `status` - Статус: `active`, `inactive`, `maintenance` (по умолчанию: `active`)
- `description` - Описание (текст)

**Пример:**

| name | code | voltage_level | length | region_code | status | description |
|------|------|---------------|--------|-------------|--------|-------------|
| ЛЭП 110 кВ Минск-Западная | LINE_110_1 | 110 | 25.5 | REG_001 | active | Основная линия |
| ЛЭП 35 кВ Минск-Северная | LINE_35_1 | 35 | 15.2 | REG_001 | active | Резервная линия |

#### 2. Импорт опор (`poles`)

**Обязательные колонки:**
- `power_line_code` - Код ЛЭП (текст, должна существовать в БД)
- `pole_number` - Номер опоры (текст)
- `latitude` - Широта (число, например: 53.9045)
- `longitude` - Долгота (число, например: 27.5615)
- `pole_type` - Тип опоры (текст: `анкерная`, `промежуточная`, `угловая` и т.д.)

**Опциональные колонки:**
- `height` - Высота, м (число)
- `foundation_type` - Тип фундамента (текст)
- `material` - Материал: `металл`, `железобетон`, `дерево` (текст)
- `year_installed` - Год установки (число)
- `condition` - Состояние: `good`, `satisfactory`, `poor` (по умолчанию: `good`)
- `notes` - Примечания (текст)

**Пример:**

| power_line_code | pole_number | latitude | longitude | pole_type | height | material | condition |
|-----------------|------------|---------|-----------|-----------|--------|----------|-----------|
| LINE_110_1 | T001 | 53.9045 | 27.5615 | анкерная | 25.5 | металл | good |
| LINE_110_1 | T002 | 53.9050 | 27.5620 | промежуточная | 22.0 | железобетон | good |

#### 3. Импорт подстанций (`substations`)

**Обязательные колонки:**
- `name` - Название подстанции (текст)
- `code` - Код подстанции (текст, уникальный)
- `voltage_level` - Уровень напряжения, кВ (число)
- `latitude` - Широта (число)
- `longitude` - Долгота (число)

**Опциональные колонки:**
- `address` - Адрес (текст)
- `region_code` - Код географического региона (текст)
- `region_name` - Название географического региона (текст)
- `description` - Описание (текст)

**Пример:**

| name | code | voltage_level | latitude | longitude | address | region_code |
|------|------|---------------|----------|-----------|---------|-------------|
| Подстанция 110/10 кВ №1 | SUB_110_1 | 110 | 53.9000 | 27.5500 | г. Минск, ул. Примерная, 1 | REG_001 |
| Подстанция 35/10 кВ №2 | SUB_35_1 | 35 | 53.9100 | 27.5600 | г. Минск, ул. Другая, 2 | REG_001 |

#### 4. Импорт оборудования (`equipment`)

**Обязательные колонки:**
- `power_line_code` - Код ЛЭП (текст, должна существовать в БД)
- `pole_number` - Номер опоры (текст, должна существовать в БД)
- `equipment_type` - Тип оборудования (текст: `изолятор`, `разрядник`, `грозозащитный трос` и т.д.)
- `name` - Название (текст)

**Опциональные колонки:**
- `manufacturer` - Производитель (текст)
- `model` - Модель (текст)
- `serial_number` - Серийный номер (текст)
- `year_manufactured` - Год изготовления (число)
- `condition` - Состояние: `good`, `satisfactory`, `poor` (по умолчанию: `good`)
- `notes` - Примечания (текст)

**Пример:**

| power_line_code | pole_number | equipment_type | name | manufacturer | model | condition |
|-----------------|-------------|----------------|------|--------------|-------|-----------|
| LINE_110_1 | T001 | изолятор | Изолятор ПС-70 | Завод №1 | ПС-70 | good |
| LINE_110_1 | T002 | разрядник | Разрядник РВО-10 | Завод №2 | РВО-10 | good |

## ✋ Ручное добавление через API

### 1. Создание ЛЭП

**Endpoint:** `POST /api/v1/power-lines`

**Пример запроса:**
```json
{
  "name": "ЛЭП 110 кВ Минск-Западная",
  "code": "LINE_110_1",
  "voltage_level": 110,
  "length": 25.5,
  "branch_id": 1,
  "status": "active",
  "description": "Основная линия"
}
```

### 2. Создание опоры

**Endpoint:** `POST /api/v1/power-lines/{power_line_id}/poles`

**Пример запроса:**
```json
{
  "pole_number": "T001",
  "latitude": 53.9045,
  "longitude": 27.5615,
  "pole_type": "анкерная",
  "height": 25.5,
  "material": "металл",
  "condition": "good"
}
```

### 3. Создание подстанции

**Endpoint:** `POST /api/v1/substations`

**Пример запроса:**
```json
{
  "name": "Подстанция 110/10 кВ №1",
  "code": "SUB_110_1",
  "voltage_level": 110,
  "latitude": 53.9000,
  "longitude": 27.5500,
  "address": "г. Минск, ул. Примерная, 1",
  "branch_id": 1,
  "description": "Основная подстанция"
}
```

### 4. Создание оборудования

**Endpoint:** `POST /api/v1/poles/{pole_id}/equipment`

**Пример запроса:**
```json
{
  "equipment_type": "изолятор",
  "name": "Изолятор ПС-70",
  "manufacturer": "Завод №1",
  "model": "ПС-70",
  "condition": "good"
}
```

## 🔐 Аутентификация

Все запросы требуют аутентификации через JWT токен.

1. **Получи токен:**
   ```bash
   POST /api/v1/auth/login
   {
     "username": "your_username",
     "password": "your_password"
   }
   ```

2. **Используй токен в заголовках:**
   ```
   Authorization: Bearer <your_token>
   ```

В Swagger UI:
1. Нажми кнопку **"Authorize"** (🔒)
2. Введи: `Bearer <your_token>`
3. Нажми **"Authorize"**

## 📝 Примеры использования

### Импорт через curl

```bash
# 1. Получи токен
TOKEN=$(curl -X POST "http://localhost/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin" | jq -r '.access_token')

# 2. Импортируй ЛЭП
curl -X POST "http://localhost/api/v1/import/power-lines" \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@power_lines.xlsx"
```

### Импорт через Python

```python
import requests

# 1. Авторизация
response = requests.post(
    "http://localhost/api/v1/auth/login",
    data={"username": "admin", "password": "admin"}
)
token = response.json()["access_token"]

# 2. Импорт
with open("power_lines.xlsx", "rb") as f:
    response = requests.post(
        "http://localhost/api/v1/import/power-lines",
        headers={"Authorization": f"Bearer {token}"},
        files={"file": f}
    )
    print(response.json())
```

## ⚠️ Важные замечания

1. **Уникальность кодов:** Коды ЛЭП и подстанций должны быть уникальными
2. **Связи:** При импорте опор и оборудования убедись, что ЛЭП уже существует
3. **Формат дат:** В Excel используй стандартный формат дат
4. **Кодировка:** Excel файлы должны быть в UTF-8
5. **Ошибки:** При импорте проверяй список ошибок в ответе API

## 📚 Дополнительная информация

- **Swagger UI:** `https://localhost/api/docs`
- **ReDoc:** `https://localhost/api/redoc`
- **Примеры Excel файлов:** См. папку `backend/examples/`

