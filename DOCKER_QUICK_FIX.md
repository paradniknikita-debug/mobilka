# Быстрое решение проблемы с Docker бэкендом

## ✅ Что было исправлено:

1. **Добавлен проброс порта 8000** в `docker-compose.yml` для прямого доступа к API
2. **Оптимизированы настройки БД** для Docker (более производительные настройки пула соединений)
3. **Создана документация** по решению проблем с Docker

## 🚀 Как использовать:

### Вариант 1: Через nginx (рекомендуется)
```
http://localhost/api/v1/test
http://localhost/docs
http://localhost/health
```

### Вариант 2: Напрямую к backend (теперь доступно)
```
http://localhost:8000/api/v1/test
http://localhost:8000/docs
http://localhost:8000/health
```

## 🔧 Перезапуск контейнеров:

После изменений в `docker-compose.yml` нужно перезапустить:

```bash
docker compose restart backend
```

Или полностью пересобрать:

```bash
docker compose down
docker compose up -d --build
```

## 📋 Проверка работы:

1. **Проверьте статус:**
   ```bash
   docker ps --filter "name=lepm"
   ```

2. **Проверьте логи:**
   ```bash
   docker logs lepm_backend --tail 20
   ```

3. **Проверьте API:**
   ```bash
   # Через nginx
   curl http://localhost/api/v1/test
   
   # Напрямую (теперь работает)
   curl http://localhost:8000/api/v1/test
   ```

## ⚠️ Важно:

- В Docker используется другой DATABASE_URL: `postgresql://postgres:dragon167@postgres:5432/lepm_db`
- Порт 8000 теперь проброшен наружу для удобства отладки
- Для продакшена можно убрать проброс порта и использовать только nginx
