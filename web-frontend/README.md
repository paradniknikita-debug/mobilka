# ЛЭП Management System - Web Frontend

Веб-фронтенд для системы управления линиями электропередач на Angular 17.

## 🚀 Быстрый старт

### Требования
- Node.js 18+ 
- npm (устанавливается с Node.js)

### Установка

1. **Установите зависимости:**
   ```bash
   cd web-frontend
   npm install
   ```

2. **Запустите проект:**
   ```bash
   npm start
   ```

3. **Откройте браузер:**
   - http://localhost:4200

### Тестовые данные для входа
- **Логин**: `admin`
- **Пароль**: `admin_123456`

## 📁 Структура проекта

```
web-frontend/
├── src/
│   ├── app/
│   │   ├── core/              # Core модуль
│   │   │   ├── guards/        # Auth guard
│   │   │   ├── interceptors/ # HTTP interceptors (Auth, Error)
│   │   │   ├── models/        # TypeScript модели данных
│   │   │   └── services/      # Сервисы (API, Auth, Map)
│   │   ├── features/          # Функциональные модули
│   │   │   ├── auth/          # Авторизация (Login)
│   │   │   ├── map/           # Карта с Leaflet
│   │   │   ├── power-lines/   # ЛЭП (заглушка)
│   │   │   ├── poles/         # Опоры (заглушка)
│   │   │   ├── equipment/     # Оборудование (заглушка)
│   │   │   └── substations/   # Подстанции (заглушка)
│   │   └── layout/            # Layout компоненты
│   │       ├── main-layout/   # Основной layout
│   │       └── sidebar/       # Боковая панель с деревом объектов
│   ├── assets/                # Статические файлы
│   └── environments/          # Конфигурация окружений
├── package.json
└── angular.json
```

## ⚙️ Конфигурация

### Переключение HTTP/HTTPS

Откройте `src/environments/environment.ts`:

```typescript
export const environment = {
  useHttps: false,  // false = HTTP, true = HTTPS
  // ...
};
```

API URL формируется автоматически:
- HTTP: `http://localhost/api/v1`
- HTTPS: `https://localhost/api/v1`

### Настройки карты

В `src/environments/environment.ts`:

```typescript
map: {
  defaultZoom: 10,
  minZoom: 1,
  maxZoom: 18,
  defaultCenter: {
    lat: 53.9045,  // Минск
    lng: 27.5615
  }
}
```

## 🎯 Реализованный функционал

✅ **Авторизация**
- Страница логина
- JWT токены
- Автоматическое перенаправление
- Обработка ошибок

✅ **Карта**
- Интеграция с Leaflet
- Отображение ЛЭП (красные линии)
- Отображение опор (синие маркеры)
- Отображение отпаек (оранжевые маркеры)
- Отображение подстанций (фиолетовые маркеры)
- Легенда карты
- Кнопка обновления данных

✅ **Sidebar (Боковая панель)**
- Расширяемое дерево объектов
- Группировка по типам (ЛЭП, Опоры, Отпайки, Подстанции)
- Счетчики объектов
- Информация об объектах

✅ **Layout**
- Material Design интерфейс
- Адаптивная боковая панель
- Меню пользователя
- Навигация

## 🔧 Технологии

- **Angular 17** - фреймворк
- **TypeScript** - язык программирования
- **Angular Material** - UI компоненты
- **Leaflet** - карты
- **RxJS** - реактивное программирование
- **RxJS Operators** - для работы с Observable

## 📦 Основные зависимости

```json
{
  "@angular/core": "^17.0.0",
  "@angular/material": "^17.0.0",
  "leaflet": "^1.9.4",
  "@asymmetrik/ngx-leaflet": "^17.0.0",
  "rxjs": "~7.8.0"
}
```

## 🛠️ Команды

```bash
# Запуск dev сервера
npm start

# Сборка для продакшена
npm run build

# Запуск тестов
npm test
```

## 🔗 Интеграция с бэкендом

Проект использует существующий FastAPI бэкенд:
- Все endpoints уже настроены
- JWT аутентификация
- REST API

## 📝 Следующие шаги

Модули для расширения:
- [ ] Power Lines - CRUD операции
- [ ] Poles - CRUD операции  
- [ ] Equipment - CRUD операции
- [ ] Substations - CRUD операции
- [ ] Фильтры и поиск
- [ ] Экспорт данных
- [ ] Графики и аналитика

## 📄 Лицензия

Внутренний проект.
