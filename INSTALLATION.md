# 📦 Инструкция по установке и запуску

## Для кого эта инструкция?

Эта инструкция для разработчиков, которые хотят:
- ✅ Клонировать проект с GitHub
- ✅ Запустить его на своей машине
- ✅ Начать работать без долгой настройки

## 🎯 Что нужно перед началом?

### Обязательно:
1. **Docker Desktop** (Windows/Mac) или **Docker + Docker Compose** (Linux)
   - Скачать: https://www.docker.com/products/docker-desktop
   - Проверить: `docker --version` и `docker compose version`

2. **Git**
   - Обычно уже установлен
   - Проверить: `git --version`

### Опционально (для frontend):
3. **Flutter SDK** (если нужен мобильный frontend)
   - Скачать: https://docs.flutter.dev/get-started/install
   - Проверить: `flutter --version`

## 🚀 Пошаговая инструкция

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/paradniknikita-debug/mobilka.git
cd mobilka
```

### Шаг 2: Автоматическая настройка

**Windows:**
```bash
setup.bat
```

**Mac/Linux:**
```bash
chmod +x setup.sh
./setup.sh
```

**Что делает скрипт:**
- ✅ Проверяет наличие Docker
- ✅ Создает `.env` файл из примера
- ✅ Генерирует SSL сертификаты для HTTPS
- ✅ Собирает и запускает все Docker контейнеры
- ✅ Применяет миграции базы данных

**Время выполнения:** ~3-5 минут (при первом запуске)

### Шаг 3: Проверка работы

Открой в браузере:
- **API тест**: https://localhost/api/v1/test
- **Swagger документация**: https://localhost/docs
- **Health check**: https://localhost/health

⚠️ **Важно**: Браузер покажет предупреждение о self-signed сертификате. Это нормально для разработки. Нажми:
- **Chrome/Edge**: "Advanced" → "Proceed to localhost (unsafe)"
- **Firefox**: "Advanced" → "Accept the Risk and Continue"

### Шаг 4 (опционально): Frontend

Если нужно запустить Flutter приложение:

```bash
cd frontend
flutter pub get
flutter run -d chrome
```

## ✅ Готово!

Проект запущен и готов к работе!

## 🔄 Повторный запуск

После первого запуска используй:

**Windows:**
```bash
start.bat
```

**Mac/Linux:**
```bash
./start.sh
```

Или вручную:
```bash
docker compose up -d
```

## 🛑 Остановка

```bash
docker compose down
```

## 📋 Полезные команды

### Просмотр логов
```bash
docker compose logs -f
```

### Применение миграций БД
```bash
docker compose exec backend alembic upgrade head
```

### Добавление тестовых данных
```bash
docker compose exec backend python seed_test_data.py
```

### Пересборка контейнеров
```bash
docker compose up -d --build
```

## ❓ Проблемы и решения

### Проблема: "Docker не найден"
**Решение**: Установи Docker Desktop и перезагрузи компьютер

### Проблема: "Порт 443 уже занят"
**Решение**: 
1. Найди процесс, использующий порт: `netstat -ano | findstr :443` (Windows)
2. Останови его или измени порт в `docker-compose.yml`

### Проблема: "SSL сертификат не создан"
**Решение**: Запусти вручную:
```bash
# Windows
nginx\generate-ssl.bat

# Mac/Linux
chmod +x nginx/generate-ssl.sh
./nginx/generate-ssl.sh
```

### Проблема: "Миграции не применены"
**Решение**: Примени вручную:
```bash
docker compose exec backend alembic upgrade head
```

### Проблема: "Frontend не подключается к backend"
**Решение**: 
1. Проверь, что backend запущен: `docker compose ps`
2. Проверь URL в `frontend/lib/core/config/app_config.dart`
3. Убедись, что используешь HTTPS (не HTTP)

## 📚 Дополнительная документация

- **Быстрый старт**: [QUICK_START.md](QUICK_START.md)
- **Требования**: [REQUIREMENTS.md](REQUIREMENTS.md)
- **Основной README**: [README.md](README.md)

## 💡 Советы

1. **Первый запуск может занять время** - Docker скачивает образы (~500 MB)
2. **Используй HTTPS** - проект настроен на HTTPS по умолчанию
3. **Проверь логи** - если что-то не работает, смотри `docker compose logs`
4. **Не коммить `.env`** - файл уже в `.gitignore`

## 🎉 Готово к разработке!

Теперь ты можешь:
- ✅ Работать с API через Swagger
- ✅ Подключаться к БД через DBeaver (порт 5432)
- ✅ Разрабатывать frontend
- ✅ Вносить изменения в код

Удачи! 🚀

