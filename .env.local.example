# ============================================
# Локальные тесты — docker-compose (корень проекта)
# ============================================
# Скопируйте в .env.local: cp .env.local.example .env.local
# Запуск: docker compose --env-file .env.local up -d
# Либо задайте переменные вручную и используйте: docker compose up -d

POSTGRES_DB=lepm_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dragon167

# Для backend в контейнере (подставляется в docker-compose.yml)
# DATABASE_URL задаётся в docker-compose для local — можно не дублировать здесь
